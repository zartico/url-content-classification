steps:
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
    args:
      - '-c'
      - >-
        echo "$BRANCH_NAME DEPLOYING";
        gsutil -m cp -r ./dags/url_content_topics/ gs://non-codecs-prod-airflow/dags/;
        gsutil -m cp -r ./src/* gs://non-codecs-prod-airflow/data/url_content_topics/src/;
        gsutil -m cp -r ./utils/* gs://non-codecs-prod-airflow/data/url_content_topics/utils/;
        gsutil -m cp -r ./config/* gs://non-codecs-prod-airflow/data/url_content_topics/config/;
    id: Deploy
    entrypoint: bash
options:
  logging: CLOUD_LOGGING_ONLY